function Wrapper(props) {
  return props.children;
  
  //----children prop holds all the content you pass b/w the
  //    opening and closing tag of your custom component.
};

export default Wrapper;